# TODO: og-image.png

`index.html` references `/og-image.png` for OpenGraph and Twitter card previews.
A 1200x630 PNG must be placed at `public/og-image.png` before launch.

Suggested composition (per `BRAND.md`):
- Background: solid `#000000`
- Centered monogram from `public/brand/monogram.svg`
- Wordmark "The Private Book" in Playfair Display 900, all-caps,
  letter-spacing 0.04em
- Subtitle "BY INVITATION ONLY" in Jost 300, letter-spacing 0.35em
- No gradients, no extra ornamentation

Until this file exists at `public/og-image.png`, share previews on
social platforms and chat apps will render blank.
